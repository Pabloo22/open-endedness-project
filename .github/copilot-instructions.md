# Python Environment
This project uses Poetry for dependency management. 
Whenever suggesting terminal commands, executing scripts, or running tests, ALWAYS prepend the command with `poetry run`. 
Never suggest raw `python`, `pip`, or `pytest` commands. 
Example: Suggest `poetry run python script.py` instead of `python script.py`.